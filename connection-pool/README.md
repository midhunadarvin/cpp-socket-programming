# Connection Pool C++

Build
```
mkdir build;
cd build;
cmake ..;
make;
```

Run
```
./build/ConnectionPool <proxy-port> <server-host> <server-port>

Eg. ./build/ConnectionPool 8080 localhost 8123
```